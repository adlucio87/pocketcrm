import 'package:flutter/material.dart';

class AppColors {
  // Dark theme
  static const darkBackground    = Color(0xFF0D0D0D);  // sfondo app
  static const darkSurface       = Color(0xFF1A1A1A);  // card, bottom nav
  static const darkSurfaceHigh   = Color(0xFF242424);  // input, chip
  static const darkBorder        = Color(0xFF2E2E2E);  // divisori, bordi
  static const darkPrimary       = Color(0xFF4F8EF7);  // accent blu elettrico
  static const darkPrimaryDark   = Color(0xFF2D6FD9);  // pressed state
  static const darkOnPrimary     = Color(0xFFFFFFFF);
  static const darkTextPrimary   = Color(0xFFEEEEEE);  // testo principale
  static const darkTextSecondary = Color(0xFF888888);  // testo secondario
  static const darkError         = Color(0xFFFF5252);  // scadenze, errori
  static const darkSuccess       = Color(0xFF4CAF50);  // completato

  // Light theme
  static const lightBackground    = Color(0xFFF4F6F9);  // sfondo app
  static const lightSurface       = Color(0xFFFFFFFF);  // card, bottom nav
  static const lightSurfaceHigh   = Color(0xFFEEF2F7);  // input, chip
  static const lightBorder        = Color(0xFFE0E4EA);  // divisori, bordi
  static const lightPrimary       = Color(0xFF2D6FD9);  // accent blu
  static const lightPrimaryDark   = Color(0xFF1A55B5);  // pressed state
  static const lightOnPrimary     = Color(0xFFFFFFFF);
  static const lightTextPrimary   = Color(0xFF1A1A2E);  // testo principale
  static const lightTextSecondary = Color(0xFF888888);  // testo secondario
  static const lightError         = Color(0xFFE53935);  // scadenze, errori
  static const lightSuccess       = Color(0xFF43A047);  // completato
}
